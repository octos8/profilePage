/* 흰 배경 */
body,
.site-header {
  background-color: #fff;
}

/* Hero 전체 영역 */
.hero {
  position: relative;
  isolation: isolate;
  display: grid;
  align-items: center;

  min-height: calc(100svh - 5.5rem);
  padding-block: clamp(6.25rem, 10vw, 11.25rem);

  background-color: #fff;
  overflow: hidden;
}

/* 중앙 콘텐츠 */
.hero-inner {
  position: relative;
  z-index: 1;

  display: flex;
  flex-direction: column;
  align-items: center;

  text-align: center;
}

.hero-title {
  max-width: 100%;
  font-family: Arial, Helvetica, sans-serif;
  font-size: clamp(2.75rem, 6.1vw, 7.375rem);
  font-weight: 900;
  line-height: 1.02;
  letter-spacing: -0.065em;
  color: #080808;
}

.hero-title-line {
  display: block;
}

.hero-title-line + .hero-title-line {
  margin-top: 0.08em;
}

.hero-description {
  max-width: 42rem;
  margin-top: 1.75rem;
  font-size: clamp(0.875rem, 1.35vw, 1.5rem);
  line-height: 1.7;
  color: #171717;
}

.hero-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;

  min-height: 3.5rem;
  margin-top: 2.25rem;
  padding: 0.875rem 3rem;

  border-radius: 999px;
  background-color: #ff481f;
  color: #fff;

  font-size: 1.125rem;
  font-weight: 600;
  line-height: 1.4;

  transition:
    background-color 0.2s ease,
    transform 0.2s ease;
}

.hero-link:hover {
  background-color: #e83912;
  transform: translateY(-2px);
}

/* 장식 요소 전체 */
.hero-decoration {
  position: absolute;
  inset: 0;
  z-index: 0;
  pointer-events: none;
  user-select: none;
}

/* 원 공통 */
.hero-circle {
  position: absolute;
  display: grid;
  place-items: center;

  aspect-ratio: 1;
  border-radius: 50%;
}

.hero-circle span {
  white-space: nowrap;
}

/* 왼쪽 위: DESIGN */
.hero-circle-design {
  top: -15%;
  left: -3%;
  width: clamp(12.5rem, 23vw, 27.5rem);

  border: 1.5px solid #171717;
}

.hero-circle-design span {
  font-size: clamp(26px, 3.4vw, 64px);
  letter-spacing: 0.02em;
  transform: rotate(-40deg);
}

/* 오른쪽 위: HTML / CSS */
.hero-circle-code {
  top: -25%;
  right: -5%;
  width: clamp(15rem, 28vw, 33.75rem);

  background-color: #eaeaea;
}

.hero-circle-code span {
  font-size: clamp(22px, 2.6vw, 48px);
  transform: translateY(80%) rotate(-25deg);
}

/* 왼쪽 아래: 회색 원 */
.hero-circle-gray {
  bottom: -10%;
  left: -8%;
  width: clamp(16.25rem, 28vw, 33.75rem);

  background-color: #eaeaea;
}

/* 왼쪽 아래: 주황색 화살표 원 */
.hero-circle-arrow {
  bottom: 15%;
  left: 11%;
  width: clamp(5.5rem, 9vw, 10.75rem);

  background-color: #ff481f;
  color: #fff;
}

.hero-circle-arrow span {
  font-size: clamp(46px, 5vw, 96px);
  line-height: 1;
}

/* 오른쪽 아래: CREATIVE */
.hero-circle-creative {
  right: -4%;
  bottom: -17%;
  width: clamp(15rem, 27vw, 32.5rem);

  border: 1.5px solid #171717;
}

.hero-circle-creative span {
  font-size: clamp(28px, 3.5vw, 66px);
  letter-spacing: 0.02em;
  transform: rotate(-30deg);
}

/* 오른쪽 작은 키워드 */
.hero-tag {
  position: absolute;
  top: 39%;
  right: 5%;

  padding: 0.5rem 1.375rem;
  border: 1.5px solid #171717;
  border-radius: 999px;

  font-size: clamp(0.875rem, 1.45vw, 1.625rem);
  line-height: 1.3;
  transform: rotate(-15deg);
}

/* 주황색 작은 점 */
.hero-dot {
  position: absolute;
  width: clamp(10px, 1.35vw, 26px);
  aspect-ratio: 1;

  border-radius: 50%;
  background-color: #ff481f;
}

.hero-dot-left {
  top: 24%;
  left: 20%;
}

.hero-dot-right {
  top: 20%;
  right: 21%;
}

.hero-dot-bottom {
  right: 6%;
  bottom: 30%;
}

/* 태블릿: 중앙 문구 주변 공간 확보 */
@media (max-width: 1024px) {
  .hero-circle-design {
    left: -10%;
  }

  .hero-circle-code {
    right: -14%;
  }

  .hero-circle-creative {
    right: -12%;
  }

  .hero-tag {
    top: auto;
    right: 6%;
    bottom: 18%;
    font-size: 16px;
  }

  .hero-description {
    max-width: 34rem;
  }
}

/* 모바일 */
@media (max-width: 600px) {
  .hero {
    min-height: calc(100svh - 72px);
    padding-block: 150px;
  }

  .hero-title {
    font-size: clamp(26px, 8vw, 48px);
    letter-spacing: -0.06em;
  }

  .hero-description {
    max-width: 28ch;
    margin-top: 24px;
    font-size: 14px;
  }

  .hero-link {
    min-height: 48px;
    margin-top: 28px;
    padding: 12px 32px;
    font-size: 14px;
  }

  .hero-circle-design {
    top: -90px;
    left: -70px;
    width: 210px;
  }

  .hero-circle-design span {
    font-size: 26px;
  }

  .hero-circle-code {
    top: -110px;
    right: -95px;
    width: 240px;
  }

  .hero-circle-code span {
    font-size: 20px;
  }

  .hero-circle-gray {
    bottom: -110px;
    left: -120px;
    width: 260px;
  }

  .hero-circle-arrow {
    bottom: 32px;
    left: 26px;
    width: 72px;
  }

  .hero-circle-arrow span {
    font-size: 44px;
  }

  .hero-circle-creative {
    right: -90px;
    bottom: -95px;
    width: 240px;
  }

  .hero-circle-creative span {
    font-size: 28px;
  }

  .hero-tag {
    display: none;
  }

  .hero-dot-left {
    top: 17%;
    left: 25%;
  }

  .hero-dot-right {
    top: 15%;
    right: 27%;
  }

  .hero-dot-bottom {
    right: 13%;
    bottom: 18%;
  }
}

@media (prefers-reduced-motion: reduce) {
  .hero-link {
    transition: none;
  }

  .hero-link:hover {
    transform: none;
  }
}

/* =========================================================
   HERO HOVER MOTION
   원/화살표 위에 마우스를 올렸을 때 각각 다른 움직임을 줍니다.
   ========================================================= */

/* hero-decoration 자체는 클릭을 막지 않고,
   실제 장식 요소만 hover를 받을 수 있게 합니다. */
.hero-circle,
.hero-tag,
.hero-dot {
  pointer-events: auto;
}

/* 공통 전환 */
.hero-circle {
  cursor: default;
  will-change: transform;
  transition:
    transform .38s cubic-bezier(.2, .8, .2, 1),
    box-shadow .38s ease,
    background-color .38s ease;
}

.hero-circle > span,
.hero-tag {
  will-change: transform;
  transition: transform .38s cubic-bezier(.2, .8, .2, 1);
}


/* 01. DESIGN 원
   살짝 위로 뜨면서 반시계 방향으로 기울어집니다. */
.hero-circle-design:hover {
  transform: translate(14px, -12px) rotate(-5deg) scale(1.035);
}

.hero-circle-design:hover span {
  transform: rotate(-34deg) scale(1.035);
}


/* 02. HTML / CSS 원
   왼쪽 아래로 부드럽게 이동하면서 살짝 회전합니다. */
.hero-circle-code:hover {
  transform: translate(-16px, 12px) rotate(5deg) scale(1.035);
  box-shadow: 0 24px 55px rgba(0, 0, 0, .08);
}

.hero-circle-code:hover span {
  transform: translateY(75%) rotate(-19deg);
}


/* 03. 왼쪽 아래 회색 원
   내용이 없어도 원 자체가 가볍게 움직입니다. */
.hero-circle-gray:hover {
  transform: translate(18px, -14px) scale(1.045);
  box-shadow: 0 24px 55px rgba(0, 0, 0, .07);
}


/* 04. 주황색 화살표
   원이 떠오르고 화살표가 오른쪽 위로 튀어나가는 느낌 */
.hero-circle-arrow {
  cursor: pointer;
}

.hero-circle-arrow:hover {
  transform: translate(10px, -14px) rotate(7deg) scale(1.08);
  box-shadow: 0 18px 40px rgba(255, 72, 31, .28);
}

.hero-circle-arrow:hover span {
  transform: translate(8px, -8px) rotate(7deg) scale(1.08);
}


/* 05. CREATIVE 원
   오른쪽 위로 이동하면서 반대 방향으로 회전 */
.hero-circle-creative:hover {
  transform: translate(-14px, -12px) rotate(5deg) scale(1.035);
}

.hero-circle-creative:hover span {
  transform: rotate(-24deg) scale(1.035);
}


/* 06. PUBLISHING 태그
   버튼처럼 위로 살짝 떠오릅니다. */
.hero-tag {
  cursor: default;
  transition:
    transform .32s cubic-bezier(.2, .8, .2, 1),
    background-color .32s ease,
    color .32s ease,
    box-shadow .32s ease;
}

.hero-tag:hover {
  transform: translateY(-8px) rotate(-10deg) scale(1.04);
  background: #ff481f;
  color: #fff;
  box-shadow: 0 12px 28px rgba(255, 72, 31, .18);
}


/* 07. 작은 주황색 점
   hover 시 통통 튀는 효과 */
.hero-dot {
  cursor: default;
  transition:
    transform .28s cubic-bezier(.2, .8, .2, 1),
    box-shadow .28s ease;
}

.hero-dot:hover {
  transform: scale(1.65);
  box-shadow: 0 0 0 8px rgba(255, 72, 31, .12);
}


/* 키보드/모션 감소 환경에서는 과한 움직임 제거 */
@media (prefers-reduced-motion: reduce) {
  .hero-circle,
  .hero-circle > span,
  .hero-tag,
  .hero-dot {
    transition: none !important;
  }

  .hero-circle:hover,
  .hero-circle:hover > span,
  .hero-tag:hover,
  .hero-dot:hover {
    transform: none !important;
  }
}

/* =========================================================
   DESIGN BANNER / INTERACTIVE ORBIT WORKS
   ========================================================= */

.design-banner {
  position: relative;
  min-height: 100svh;
  overflow: hidden;
  isolation: isolate;
  background: #171717;
  color: #fff;
}

.design-banner-bg,
.design-banner-overlay {
  position: absolute;
  inset: 0;
}

.design-banner-bg {
  z-index: -3;
  background:
    linear-gradient(110deg, rgba(0,0,0,.08), rgba(0,0,0,.12)),
    url("../img/profile-workspace.jpg") center / cover no-repeat;
  transform: scale(1.03);
}

.design-banner-overlay {
  z-index: -2;
  background:
    linear-gradient(90deg, rgba(0,0,0,.58) 0%, rgba(0,0,0,.2) 48%, rgba(0,0,0,.48) 100%);
}

.design-banner-inner {
  position: relative;
  min-height: 100svh;
}

.design-banner-content {
  position: absolute;
  z-index: 4;
  top: 50%;
  left: 0;
  width: min(34rem, 34vw);
  transform: translateY(-50%);
}

.design-banner-content h2 {
  color: #fff;
  font-size: clamp(3.2rem, 4.6vw, 5.6rem);
  font-weight: 800;
  line-height: .93;
  letter-spacing: -.055em;
}

.design-banner-description {
  margin-top: 1.35rem;
  color: rgba(255,255,255,.92);
  font-size: clamp(.9rem, 1vw, 1.15rem);
  line-height: 1.45;
}

.design-banner .section-link {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 1.3rem;
  min-width: 11rem;
  min-height: 3rem;
  margin-top: 1.8rem;
  padding: .75rem 1.5rem;
  border-radius: 999px;
  background: #ff481f;
  color: #fff;
  font-size: .78rem;
  font-weight: 700;
  transition: transform .25s ease, background-color .25s ease;
}

.design-banner .section-link:hover {
  background: #e83912;
  transform: translateY(-3px);
}


/* 작품 미리보기 */

.design-work-preview {
  position: absolute;
  z-index: 5;
  top: 51%;
  left: 45%;
  width: clamp(23rem, 28vw, 31rem);
  transform: translate(-50%, -50%);
}

.design-work-preview-media {
  overflow: hidden;
  aspect-ratio: 16 / 9;
  border: 3px solid rgba(255,255,255,.78);
  border-radius: 1.35rem;
  background: #ddd;
  box-shadow: 0 1.5rem 4rem rgba(0,0,0,.28);
}

.design-work-preview-media img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition:
    opacity .2s ease,
    transform .45s cubic-bezier(.2,.8,.2,1);
}

.design-work-preview-media.is-changing img {
  opacity: 0;
  transform: scale(1.06);
}

.design-work-preview:hover .design-work-preview-media img {
  transform: scale(1.035);
}

.design-work-preview-info {
  margin-top: .9rem;
  padding-left: .2rem;
  color: #fff;
}

.design-work-preview-info > p:first-child {
  color: #ff6b48;
  font-size: .64rem;
  font-weight: 700;
  letter-spacing: .24em;
}

.design-work-preview-info h3 {
  margin-top: .25rem;
  font-size: 1.25rem;
  line-height: 1.15;
}

#designPreviewDescription {
  margin-top: .32rem;
  color: rgba(255,255,255,.7);
  font-size: .78rem;
  line-height: 1.45;
}

#designPreviewLink {
  display: inline-flex;
  align-items: center;
  gap: .6rem;
  margin-top: .75rem;
  padding: .5rem .9rem;
  border: 1px solid rgba(255,255,255,.5);
  border-radius: 999px;
  color: #fff;
  font-size: .7rem;
  font-weight: 600;
  transition:
    background-color .22s ease,
    border-color .22s ease,
    transform .22s ease;
}

#designPreviewLink:hover {
  border-color: #ff481f;
  background: #ff481f;
  transform: translateY(-2px);
}


/* 오른쪽 원형 메뉴 */

.design-orbit {
  position: absolute;
  z-index: 6;
  top: 50%;
  right: 0;
  width: clamp(24rem, 28vw, 31rem);
  aspect-ratio: 1;
  transform: translateY(-50%);
}

.design-orbit-circle {
  position: absolute;
  inset: 13%;
  border: 1px solid rgba(255,255,255,.18);
  border-radius: 50%;
  pointer-events: none;
}

.design-orbit-item {
  position: absolute;
  z-index: 2;
  border: 0;
  padding: .25rem .45rem;
  background: transparent;
  color: rgba(255,255,255,.84);
  font: inherit;
  font-size: clamp(.95rem, 1.15vw, 1.3rem);
  white-space: nowrap;
  cursor: pointer;
  transition:
    color .28s ease,
    transform .28s cubic-bezier(.2,.8,.2,1),
    text-shadow .28s ease,
    opacity .22s ease;
}

.design-orbit-item::before {
  content: "";
  position: absolute;
  width: .32rem;
  aspect-ratio: 1;
  border-radius: 50%;
  background: #fff;
  transition:
    background-color .25s ease,
    transform .25s ease,
    box-shadow .25s ease;
}

.design-orbit-item:hover,
.design-orbit-item:focus-visible,
.design-orbit-item.is-active {
  z-index: 5;
  color: #fff;
  text-shadow: 0 0 1.4rem rgba(255,255,255,.35);
}

.design-orbit-item:hover::before,
.design-orbit-item:focus-visible::before,
.design-orbit-item.is-active::before {
  background: #ff481f;
  transform: scale(1.7);
  box-shadow: 0 0 0 .35rem rgba(255,72,31,.12);
}

.orbit-ui {
  top: 4%;
  left: 37%;
  transform: rotate(6deg);
}
.orbit-ui::before {
  top: 50%;
  left: -1.8rem;
}

.orbit-publishing {
  top: 10%;
  left: 18%;
  font-size: clamp(.95rem, 1.15vw, 1.3rem);
  font-weight: 400;
  transform: rotate(-24deg);
}
.orbit-publishing::before {
  top: 50%;
  right: -1.8rem;
}

.orbit-branding {
  top: 23%;
  right: -1%;
  transform: rotate(57deg);
}
.orbit-branding::before {
  top: 50%;
  left: -1.8rem;
}

.orbit-popup {
  right: 0;
  bottom: 20%;
  transform: rotate(-56deg);
}
.orbit-popup::before {
  top: 50%;
  left: -1.8rem;
}

.orbit-poster {
  bottom: 3%;
  left: 45%;
  transform: rotate(8deg);
}
.orbit-poster::before {
  top: 50%;
  left: -1.8rem;
}

.orbit-detail {
  left: 0;
  bottom: 18%;
  transform: rotate(60deg);
}
.orbit-detail::before {
  top: 50%;
  right: -1.8rem;
}

.orbit-projects {
  left: 0;
  top: 24%;
  transform: rotate(-60deg);
}
.orbit-projects::before {
  top: 50%;
  right: -1.8rem;
}

/* 원래 회전값을 유지하면서 hover 확대 */
.orbit-ui:hover,
.orbit-ui:focus-visible { transform: rotate(6deg) scale(1.12); }

.orbit-ui.is-active { transform: rotate(6deg); }

.orbit-branding:hover,
.orbit-branding:focus-visible { transform: rotate(57deg) scale(1.12); }

.orbit-branding.is-active { transform: rotate(57deg); }

.orbit-popup:hover,
.orbit-popup:focus-visible { transform: rotate(-56deg) scale(1.12); }

.orbit-popup.is-active { transform: rotate(-56deg); }

.orbit-poster:hover,
.orbit-poster:focus-visible { transform: rotate(8deg) scale(1.12); }

.orbit-poster.is-active { transform: rotate(8deg); }

.orbit-detail:hover,
.orbit-detail:focus-visible { transform: rotate(60deg) scale(1.12); }

.orbit-detail.is-active { transform: rotate(60deg); }

.orbit-projects:hover,
.orbit-projects:focus-visible { transform: rotate(-60deg) scale(1.12); }

.orbit-projects.is-active { transform: rotate(-60deg); }

.orbit-publishing:hover,
.orbit-publishing:focus-visible { transform: rotate(-24deg) scale(1.12); }

.orbit-publishing.is-active { transform: rotate(-24deg); }

.design-orbit-center {
  position: absolute;
  left: 50%;
  top: 50%;
  z-index: 4;

  width: 58%;
  transform: translate(-50%, -50%);

  color: #fff;
  font-size: clamp(1.45rem, 1.85vw, 2.2rem);
  font-weight: 500;
  line-height: 1.1;
  text-align: center;
  white-space: normal;

  pointer-events: none;

  transition:
    opacity .2s ease,
    transform .3s ease;
}


/* 글자가 바뀔 때 살짝 움직임 */

.design-orbit-center.is-changing {
    opacity: 0;

    transform:
        translate(-50%, -50%)
        scale(0.85);
}

@media (max-width: 1150px) {
  .design-banner-content {
    width: 32rem;
  }

  .design-work-preview {
    left: 48%;
    width: 24rem;
  }

  .design-orbit {
    right: -5rem;
    transform: translateY(-50%) scale(.82);
  }
}


@media (max-width: 820px) {
  .design-banner {
    min-height: auto;
    padding: 7rem 0 5rem;
  }

  .design-banner-inner {
    display: grid;
    min-height: auto;
    gap: 2.2rem;
  }

  .design-banner-content,
  .design-work-preview,
  .design-orbit {
    position: relative;
    inset: auto;
    width: 100%;
    transform: none;
  }

  .design-banner-content h2 {
    font-size: clamp(3rem, 11vw, 4.8rem);
  }

  .design-work-preview {
    max-width: 37rem;
  }

  .design-orbit {
    display: flex;
    flex-wrap: wrap;
    gap: .55rem;
    aspect-ratio: auto;
  }

  .design-orbit-circle {
    display: none;
  }

  .design-orbit-center {
    display: none;
  }

  .design-orbit-item,
  .orbit-ui,
  .orbit-publishing,
  .orbit-branding,
  .orbit-popup,
  .orbit-poster,
  .orbit-detail,
  .orbit-projects {
    position: relative;
    inset: auto;
    transform: none;
    padding: .6rem .85rem;
    border: 1px solid rgba(255,255,255,.35);
    border-radius: 999px;
    font-size: .78rem;
  }

  .design-orbit-item::before {
    display: none;
  }

  .design-orbit-item:hover,
  .design-orbit-item:focus-visible,
  .design-orbit-item.is-active,
  .orbit-ui:hover,
  .orbit-branding:hover,
  .orbit-popup:hover,
  .orbit-poster:hover,
  .orbit-detail:hover,
  .orbit-projects:hover,
  .orbit-publishing:hover {
    transform: none;
    border-color: #ff481f;
    background: #ff481f;
  }
}


@media (prefers-reduced-motion: reduce) {
  .design-work-preview-media img,
  .design-orbit-item,
  .design-orbit-item::before,
  #designPreviewLink {
    transition: none !important;
  }
}
