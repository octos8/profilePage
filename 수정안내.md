# 개발자 도구로 스타일 수정하기

1. 페이지에서 수정할 글자나 이미지를 우클릭하고 **검사**를 선택합니다.
2. **Elements(요소) → Styles(스타일)**에서 `font-size`, `width`, `margin`, `padding`, `color` 등의 값을 바꿉니다. 예: `font-size: 32px;`의 숫자를 키우면 글자가 커집니다.
3. 마음에 드는 결과가 나오면 스타일 오른쪽의 CSS 파일명과 줄 번호를 확인하고, 편집기에서 해당 파일의 같은 규칙을 수정해 저장합니다.
4. 새로고침하여 저장한 결과를 확인합니다. 개발자 도구에서만 바꾼 값은 보통 새로고침하면 사라집니다.

## 어느 파일을 수정하나요?

| 화면 영역 | 파일 |
| --- | --- |
| 공통 색상, 기본 글꼴 크기, 페이지 폭 | `css/root.css` |
| 상단 메뉴 | `css/header.css` |
| 첫 화면 제목·원 장식, 디자인 배너 | `css/index.css` |
| Creative Works와 첫 화면 스크롤 전환 | `css/intro.css` |
| 프로필·스킬 | `css/profile.css` |
| 디자인 작업의 PC·태블릿·모바일 목업 | `css/visual-works.css` |
| 웹 프로젝트 카드 | `css/web-projects.css` |

## 화면 크기에 따른 수정

크기 설정은 `px` 숫자와 `@media` 규칙으로 나누었습니다. 기존의 `rem`, `%`, `vw` 설정도 다른 속성에 남아 있습니다.

`@media (max-width: 820px)` 안의 규칙은 브라우저 너비가 820px 이하일 때 적용됩니다. 모바일에서만 바꾸려면 해당 규칙 안의 숫자를 수정하세요. Styles에서 취소선이 있는 값은 다른 규칙이 덮어쓴 값이므로, 현재 적용 중인 규칙을 수정하세요.

## JavaScript가 제어하는 부분

- 디자인 목업과 웹 프로젝트 카드 일부는 각각 `js/visual-works.js`, `js/web-projects.js`가 생성합니다. 생성된 요소도 개발자 도구에서 검사할 수 있습니다. 이미지·링크를 영구 변경하려면 해당 JavaScript의 내용을 수정합니다.
- 첫 화면의 투명도·이동과 배너의 회전 메뉴 위치는 `js/index.js`가 갱신합니다. 회전 메뉴의 `left`, `top`은 개발자 도구에서 바꿔도 다시 덮어써질 수 있습니다. 원의 간격은 JavaScript의 `radiusX`, `radiusY`에서 조절합니다.
- 웹 프로젝트 카드의 마우스 반응 이동은 `js/web-projects.js`가 제어합니다.
- 일부 모바일 배치에는 `!important`가 있습니다. 값이 적용되지 않으면 Styles에서 해당 규칙과 `element.style`도 확인하세요.

별도 빌드 없이 HTML에서 CSS와 JavaScript를 직접 불러오는 구조입니다. 일반적인 글자 크기·색상·여백은 CSS에서 바로 수정할 수 있습니다.
